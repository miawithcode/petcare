export type Review = {
  review: string;
  owner: string;
  store: string;
  imageUrl: string;
};

export type Pet = {
  id: string;
  name: string;
  ownerName: string;
  imageUrl: string;
  age: number;
  notes: string;
};
